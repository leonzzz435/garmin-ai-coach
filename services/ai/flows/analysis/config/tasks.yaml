metrics_task:
  name: metrics_analysis
  description: >
    Analyze training metrics data according to the Metrics Agent role defined below.
    Focus on macro-level performance metrics including training load and VO2max trends.

    Style Guide:
    ```markdown
    {style_guide}
    ```

    Agent Roles:
    ```markdown
    {agent_roles}
    ```

    Training Metrics:
    ```json
    {data}
    ```

    Upcoming Competitions:
    ```json
    {competitions}
    ```

    Current Date:
    ```json
    {current_date}
    ```

    Follow your role as defined in the "1. Metrics Agent" section of Agent Roles.
  expected_output: >
    A comprehensive analysis of performance metrics and their implications for competition preparation.
    Must include a Metrics Readiness Score (0-100) based on training load, ACWR, and adaptation metrics.
    Format according to the Style Guide specifications provided above.
  output_file: stuff/analysis/metrics.md
  agent: metrics_agent

activities_task:
  name: activity_analysis
  description: >
    Analyze workout execution data according to the Activity Agent role defined below.
    Focus on session-level details including heart rate zones, intervals, and compliance.

    Style Guide:
    ```markdown
    {style_guide}
    ```

    Agent Roles:
    ```markdown
    {agent_roles}
    ```

    Activity Data:
    ```json
    {data}
    ```

    Upcoming Competitions:
    ```json
    {competitions}
    ```

    Current Date:
    ```json
    {current_date}
    ```

    Follow your role as defined in the "2. Activity Agent" section of Agent Roles.
  expected_output: >
    A detailed analysis of training activities and their effectiveness for race preparation.
    Format according to the Style Guide specifications provided above.
  output_file: stuff/analysis/activities.md
  agent: activity_agent

physiology_task:
  name: physio_analysis
  description: >
    Analyze physiological data according to the Physiology Agent role defined below.
    Focus on recovery markers, HRV trends, and overall readiness indicators.

    Style Guide:
    ```markdown
    {style_guide}
    ```

    Agent Roles:
    ```markdown
    {agent_roles}
    ```

    Physiological Data:
    ```json
    {data}
    ```

    Upcoming Competitions:
    ```json
    {competitions}
    ```

    Current Date:
    ```json
    {current_date}
    ```

    Follow your role as defined in the "3. Physiology Agent" section of Agent Roles.
  expected_output: >
    A thorough analysis of physiological state and readiness for competition.
    Must include a Physiology Readiness Score (0-100) based on HRV, sleep quality, and stress levels.
    Format according to the Style Guide specifications provided above.
  output_file: stuff/analysis/physiology.md
  agent: physiology_agent

synthesis_task:
  name: synthesis
  description: >
    Synthesize parallel analyses according to the Synthesis Agent role defined below.
    Focus on integrating metrics, activities, and physiology insights for {athlete_name}.

    Style Guide:
    ```markdown
    {style_guide}
    ```

    Agent Roles:
    ```markdown
    {agent_roles}
    ```

    Upcoming Competitions:
    ```json
    {competitions}
    ```

    Current Date:
    ```json
    {current_date}
    ```

    Follow your role as defined in the "4. Synthesis Agent" section of Agent Roles.
  expected_output: >
    A cohesive synthesis report integrating all analysis components.
    Format according to the Style Guide specifications provided above.
  output_file: stuff/analysis/synthesis.md
  agent: synthesis_agent
